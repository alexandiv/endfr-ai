# AGENTS.md

Программа обработки ядерных данных для использование в программах
Монте-Карло. Входные файлы — исходная библиотека и результат их
прогона через модуль `reconr` программы `njoy2016`. Для теста
используются данные для водорода из B8.1: n-001_H_001.endf,
n-001_H_001.reconr. Для чтения пишется парсер формата ENDF-6.

## Файлы

- `main.cpp` — весь код парсера (пока в одном файле, `namespace endf`).
- `n-001_H_001.endf` — исходный файл ENDF (H-1).
- `n-001_H_001.reconr` — он же после `njoy2016/reconr` (рабочий вход).
- `CMakeLists.txt`, `CMakePresets.json` — сборка (пресеты `GCC_Debug`,
  `GCC_Release`).
- `.clang-format` — стиль; форматировать обязательно перед коммитом.
- `.vscode/settings.json` - установка для редактора vscode

## Сборка

```
cmake --preset GCC_Debug
cmake --build build/GCC_Debug
```

## Запуск

- запуск и тестирование я делаю сам

## Стиль кода

- Используем **минимальное подмножество C++**. Код должен оставаться
близким к C с контейнерами STL.
- Следим за скоростью используемых алгоритмов.
- Не допускаем лишних аллокаций памяти и создания временных копий обектов.
- Память желательно должна быть "сплошной" - то есть одним куском.
- в парсере только только C stdio, char[].
- `std::string` — допустимо в коде ввода/вывода и конфигурации
  (чтение input, генерация скриптов и т.д.).
- `sscanf` в `read_input` — осознанно оставлен для парсинга чисел:
  формат `" %d"/" %lf"` надёжно пропускает ведущие пробелы,
  в отличие от `std::from_chars`.

Явно **не использовать**:

- классы с инкапсуляцией (`private`/`protected`), только `struct` с
  публичными полями;
- виртуальные функции, наследование, полиморфизм;
- RTTI (`dynamic_cast`, `typeid`);
- исключения — ошибки через `fprintf(stderr, ...)` + `exit(EXIT_FAILURE)`;
- лямбды, `auto` без нужды, range-based for там, где обычный `for` яснее;
- умные указатели, RAII-обёртки — сырые `FILE*`, обычные массивы;
- операторные перегрузки, конструкторы копирования/перемещения,
  пользовательские деструкторы (пусть генерируются по умолчанию);

Разрешено:
- C++ standard library
- namespaces
- template functions

## Конвенции

- Имена функций/полей/переменных — `snake_case`, константы — `UPPER_SNAKE`.
- Функции парсера ENDF - в namespace endf.
- Функции объявляются сверху файла, реализации ниже.
- Комментарии на русском.
- Форматирование строго по `.clang-format`.

